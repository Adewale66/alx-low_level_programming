#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Programme for printing the alphabet
 *
 * Return: always 0
 */

int main(void)
{
	for (int i = 97; i < 123; i++)
		putchar(i);
	putchar('\n');
	return (0);
}

