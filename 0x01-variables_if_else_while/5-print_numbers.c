#include <stdio.h>

/**
 * main - Entry point
 *
 *Description: Prints numbers from 0 - 9
 *
 * Return: 0
 */

int main(void)
{
	for (int i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
